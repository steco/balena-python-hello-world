version: "2"

services:
  balena-hello-world:
    network_mode: "host"
    privileged: true
    build: .
    ports:
      - "80:80"
    volumes:
      - /run/dbus:/host/run/dbus:ro
    labels:
      io.balena.features.dbus: "1"
